{
  "name": "ai-workflows-site-starter",
  "private": true,
  "version": "0.1.0",
  "description": "EN+RU starter site for AI workflows (Eleventy) for Cloudflare Pages",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
